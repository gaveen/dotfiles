# ~/.bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# User specific environment and startup programs

#export EDITOR='nvim'
#export GIT_EDITOR='nvim'

# Automatically added by 'rustup' for Cargo
#. "$HOME/.cargo/env"
